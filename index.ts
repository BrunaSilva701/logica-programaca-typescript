// projeto para compilar e executar codigo
// Criar uma fnção que aceita dois argumentos do tipo numerico
// Exibe a soma entre os dois

function Soma (a: number, b:number){
    this.a = a;
    this.b = b;
}

let soma = new Soma(a+b);